# cli

## 目录定位
命令行测试入口目录。

## 职责范围
- 通用测试启动入口
- 命令行执行参数承接

## 允许放入
- `run_test.gd`
- 与 CLI 测试入口直接相关的薄壳脚本

## 禁止放入
- 具体测试逻辑
- 报告产物
- 旧 phase 运行器包装

## 对外依赖
- 可依赖 `res://tests/helpers/` 与具体测试目录
- 不承载业务实现

## 维护约束
- `run_test.gd` 是唯一通用入口
- 不复制多份入口壳
- appdata 与日志不入库
