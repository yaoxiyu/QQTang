# tests

## 目录定位
统一测试主目录。

## 子目录职责
- `unit/`：单元测试。
- `integration/`：集成链路测试。
- `contracts/`：路径、配置与运行时契约测试。
- `smoke/`：长链路冒烟验证。
- `helpers/`：测试辅助脚本。
- `cli/`：命令行测试入口。
- `scripts/`：测试运行脚本与测试壳。

## 维护规则
- 只保留类型化测试结构，不再继续按阶段拆测试目录。
- 正式业务代码不能反向依赖测试目录。
