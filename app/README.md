# app

## 目录定位
应用级流程编排与全局运行时入口层。

## 子目录职责
- `flow/`：前台流程、启动编排、运行期根配置与入口切换。
- `debug/`：显式调试工具与开发期开关，不作为正式默认入口。

## 维护规则
- 这里只负责流程和全局编排，不承载 battle 规则、transport 细节或表现层真相。
- 新增子目录时，必须服从“流程编排 / 调试工具”这两个一级语义。
